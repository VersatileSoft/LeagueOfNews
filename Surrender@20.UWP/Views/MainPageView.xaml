<m:MvxWindowsPage
    xmlns:m="using:MvvmCross.Platforms.Uap.Views"
    x:Class="Surrender_20.MainPageView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:appmodel="using:Windows.ApplicationModel"
    xmlns:i="using:Microsoft.Xaml.Interactivity"
    xmlns:core="using:Microsoft.Xaml.Interactions.Core"
    mc:Ignorable="d"
    Background="{ThemeResource SystemControlChromeLowAcrylicWindowBrush}">
    <m:MvxWindowsPage.Transitions>
        <TransitionCollection>
            <EntranceThemeTransition/>
        </TransitionCollection>
    </m:MvxWindowsPage.Transitions>
    
    <Grid>
        <Grid x:Name="AppTitleBar" VerticalAlignment="Top" Background="{ThemeResource SystemControlChromeMediumAcrylicWindowMediumBrush}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition x:Name="LeftPaddingColumn" Width="0"/>
                <ColumnDefinition/>
                <ColumnDefinition x:Name="RightPaddingColumn" Width="0"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="{x:Bind appmodel:Package.Current.DisplayName}" 
                       Grid.Column="1" 
                       Style="{StaticResource CaptionTextBlockStyle}" 
                       Margin="10,8,0,0"/>
        </Grid>
       
        <NavigationView x:Name="NavView"
                        OpenPaneLength="200"
                        Margin="0,30,0,0"
                        IsSettingsVisible="True"
                        IsBackButtonVisible="Collapsed">

            <NavigationView.MenuItems>
                <NavigationViewItemHeader Content="Pages"/>
                <NavigationViewItem Icon="Home" Content="Home" x:Name="HomePage">
                    <!--
                    <i:Interaction.Behaviors>
                        <core:EventTriggerBehavior EventName="Tapped">
                            <core:InvokeCommandAction Command="{x:Bind VM.HomeCommand}"/>
                        </core:EventTriggerBehavior>
                    </i:Interaction.Behaviors>
                    -->
                </NavigationViewItem>
                
                <NavigationViewItem Content="PBE">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph=""/>
                    </NavigationViewItem.Icon>
                    <!--
                    <i:Interaction.Behaviors>
                        <core:EventTriggerBehavior EventName="Tapped">
                            <core:InvokeCommandAction Command="{x:Bind VM.PBECommand}"/>
                        </core:EventTriggerBehavior>
                    </i:Interaction.Behaviors>
                    -->
                </NavigationViewItem>

                <NavigationViewItem Content="Releases" x:Name="ReleasesPage">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph=""/>
                    </NavigationViewItem.Icon>
                    <!--
                    <i:Interaction.Behaviors>
                        <core:EventTriggerBehavior EventName="Tapped">
                            <core:InvokeCommandAction Command="{x:Bind VM.ReleasesCommand}"/>
                        </core:EventTriggerBehavior>
                    </i:Interaction.Behaviors>
                    -->
                </NavigationViewItem>
                
                <NavigationViewItem Content="Red Posts" x:Name="RedPostsPage">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xEC7A;"/>
                    </NavigationViewItem.Icon>
                    <!--
                    <i:Interaction.Behaviors>
                        <core:EventTriggerBehavior EventName="Tapped">
                            <core:InvokeCommandAction Command="{x:Bind VM.RedPostsCommand}"/>
                        </core:EventTriggerBehavior>
                    </i:Interaction.Behaviors>
                    -->
                </NavigationViewItem>

                <NavigationViewItem Icon="People" Content="Rotations" x:Name="RotationsPage">
                    <!--
                    <i:Interaction.Behaviors>
                        <core:EventTriggerBehavior EventName="Tapped">
                            <core:InvokeCommandAction Command="{x:Bind VM.RotationsCommand}"/>
                        </core:EventTriggerBehavior>
                    </i:Interaction.Behaviors>
                    -->
                </NavigationViewItem>

                <NavigationViewItem Content="E-Sports" x:Name="EsportsPage">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph=""/>
                    </NavigationViewItem.Icon>
                    <!--
                    <i:Interaction.Behaviors>
                        <core:EventTriggerBehavior EventName="Tapped">
                            <core:InvokeCommandAction Command="{x:Bind VM.EsportsCommand}"/>
                        </core:EventTriggerBehavior>
                    </i:Interaction.Behaviors>
                    -->
                </NavigationViewItem>
            </NavigationView.MenuItems>

            <NavigationView.Header>
                <Grid Margin="24,10,10,0">
                    <CommandBar DefaultLabelPosition="Right" Background="Transparent">
                        <CommandBar.PrimaryCommands>
                            <AppBarButton Icon="Refresh" Label="Refresh"/>
                        </CommandBar.PrimaryCommands>
                        
                        <CommandBar.Content>
                            <TextBlock Text="{x:Bind VM.Title}" Margin="12,14"/>
                        </CommandBar.Content>
                    </CommandBar>
                </Grid>
            </NavigationView.Header>
            <Frame x:Name="DefaultFrame" Margin="24,0,24,0">
                <Image Source="ms-appx:///Assets/LargeTile.scale-400.png" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Frame>
        </NavigationView>
    </Grid>
</m:MvxWindowsPage>
