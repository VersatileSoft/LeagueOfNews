<m:MvxWindowsPage
    xmlns:m="using:MvvmCross.Platforms.Uap.Views"
    x:Class="Surrender_20.MainPageView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:appmodel="using:Windows.ApplicationModel"
    xmlns:i="using:Microsoft.Xaml.Interactivity"
    xmlns:core="using:Microsoft.Xaml.Interactions.Core"
    mc:Ignorable="d"
    Background="{ThemeResource SystemControlChromeLowAcrylicWindowBrush}">
    <m:MvxWindowsPage.Transitions>
        <TransitionCollection>
            <EntranceThemeTransition/>
        </TransitionCollection>
    </m:MvxWindowsPage.Transitions>
    
    <Grid>
        <Grid x:Name="DragArea" Margin="50,0,0,0" VerticalAlignment="Top" Height="45"/>

        <NavigationView x:Name="NavView"
                        OpenPaneLength="200"
                        IsSettingsVisible="True"
                        IsBackButtonVisible="Collapsed"
                        SelectionChanged="NavView_ItemSelected"
                        PaneTitle="{x:Bind appmodel:Package.Current.DisplayName}">

            <NavigationView.MenuItems>
                <NavigationViewItemHeader Content="Pages"/>

                <NavigationViewItem Icon="Home" Content="Home" x:Name="HomePage"/>

                <NavigationViewItem Content="PBE">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph=""/>
                    </NavigationViewItem.Icon>
                </NavigationViewItem>

                <NavigationViewItem Content="Releases" x:Name="ReleasesPage">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph=""/>
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                
                <NavigationViewItem Content="Red Posts" x:Name="RedPostsPage">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xEC7A;"/>
                    </NavigationViewItem.Icon>
                </NavigationViewItem>

                <NavigationViewItem Icon="People" Content="Rotations" x:Name="RotationsPage"/>

                <NavigationViewItem Content="E-Sports" x:Name="EsportsPage">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph=""/>
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
            </NavigationView.MenuItems>

            <NavigationView.Header>
                <Grid Margin="20,40,20,0">
                    <CommandBar DefaultLabelPosition="Right" Background="Transparent">
                        <CommandBar.PrimaryCommands>
                            <AppBarButton Icon="Refresh" Label="Refresh"/>
                        </CommandBar.PrimaryCommands>
                        
                        <CommandBar.Content>
                            <TextBlock Text="{x:Bind VM.Title}" Margin="12,14"/>
                        </CommandBar.Content>
                    </CommandBar>
                </Grid>
            </NavigationView.Header>
            <Frame x:Name="ContentFrame" Margin="20,0,20,0">
                <Image Source="ms-appx:///Assets/LargeTile.scale-400.png" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Frame>
        </NavigationView>
    </Grid>
</m:MvxWindowsPage>
