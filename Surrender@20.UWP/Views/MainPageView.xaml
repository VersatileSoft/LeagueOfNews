<m:MvxWindowsPage
    xmlns:m="using:MvvmCross.Platforms.Uap.Views"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:appmodel="using:Windows.ApplicationModel"
    xmlns:i="using:Microsoft.Xaml.Interactivity"
    xmlns:core="using:Microsoft.Xaml.Interactions.Core"
    xmlns:conv="using:Surrender_20.UWP.Converters"
    x:Class="Surrender_20.UWP.View.MainPageView"
    mc:Ignorable="d"
    Background="{ThemeResource SystemControlAcrylicWindowMediumHighBrush}">
    <m:MvxWindowsPage.Resources>
        <conv:SelectedItemToStringConverter x:Key="SelectedItemToString"/>
    </m:MvxWindowsPage.Resources>

    <Grid>
        <Grid x:Name="DragArea" Background="Transparent" HorizontalAlignment="Left" Height="34" VerticalAlignment="Top" Width="1689" Canvas.ZIndex="5">
            <Image Source="ms-appx:///Assets/Square44x44Logo.scale-100.png" 
                   HorizontalAlignment="Left" 
                   Width="20" 
                   Height="20" 
                   Margin="12,0"/>
            <TextBlock Text="{x:Bind appmodel:Package.Current.DisplayName}" FontSize="12" Margin="44,8" Style="{StaticResource CaptionTextBlockStyle}" HorizontalAlignment="Left" VerticalAlignment="Top"/>
        </Grid>

        <NavigationView x:Name="NavigationBar"
                        IsSettingsVisible="False"
                        IsBackButtonVisible="Collapsed"
                        PaneDisplayMode="Top"
                        SelectionFollowsFocus="Enabled">
            <i:Interaction.Behaviors>
                <core:EventTriggerBehavior EventName="SelectionChanged">
                    <core:InvokeCommandAction Command="{Binding NavCommand}" InputConverter="{StaticResource SelectedItemToString}"/>
                </core:EventTriggerBehavior>
            </i:Interaction.Behaviors>
            <NavigationView.MenuItems>
                <NavigationViewItem Icon="Home" Content="Home"/>
                
                <NavigationViewItem Content="PBE">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph=""/>
                    </NavigationViewItem.Icon>
                </NavigationViewItem>

                <NavigationViewItem Content="Releases">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph=""/>
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                
                <NavigationViewItem Content="Red Posts">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xEC7A;"/>
                    </NavigationViewItem.Icon>
                </NavigationViewItem>

                <NavigationViewItem Icon="People" Content="Rotations"/>

                <NavigationViewItem Content="E-Sports">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph=""/>
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
            </NavigationView.MenuItems>
           
            <Frame x:Name="ContentFrame">
                <Image Source="ms-appx:///Assets/LargeTile.scale-400.png" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Frame>
        </NavigationView>
    </Grid>
</m:MvxWindowsPage>
