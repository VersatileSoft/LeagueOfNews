<?xml version="1.0" encoding="utf-8" ?>
<views:MvxContentPage xmlns:views="clr-namespace:MvvmCross.Forms.Views;assembly=MvvmCross.Forms" xmlns="http://xamarin.com/schemas/2014/forms"
                      xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                      x:Class="Surrender@20.Forms.Views.NewsfeedItemView"
                      xmlns:mvx="clr-namespace:MvvmCross.Forms.Bindings;assembly=MvvmCross.Forms"
                      xmlns:viewModels="clr-namespace:Surrender_20.Core.ViewModels;assembly=Surrender_20.Core"
                      Title="{Binding Path=Data.Title}">
    <views:MvxContentPage.Resources>
        <ResourceDictionary>
            <!--<conv:UriToImageSource x:Key="UriToImageSource"/>-->
        </ResourceDictionary>
    </views:MvxContentPage.Resources>

    <views:MvxContentPage.Content>
        <ScrollView>
            <Grid>
                <ActivityIndicator VerticalOptions="Center" 
                                   HorizontalOptions="Center"
                                   IsRunning="True"
                                   IsVisible="False"/>

                <StackLayout IsVisible="True">

                    <Image Source="{Binding Path=Data.Image, Converter={StaticResource UriToImageSource}}" 
                           Aspect="AspectFill"  
                           HeightRequest="270"
                           HorizontalOptions="FillAndExpand"
                           VerticalOptions="Start"/>
                    <StackLayout Margin="20,10,20,10">
                        <Label Text="{Binding Data.Title}" 
                               FontAttributes="Bold"          
                               WidthRequest="1000"/>
                        <ContentView Content="{Binding ContentDescription}"/>
                    </StackLayout>
                </StackLayout>
            </Grid>
        </ScrollView>
    </views:MvxContentPage.Content>
</views:MvxContentPage>