<?xml version="1.0" encoding="utf-8" ?>
<views:MvxContentPage x:TypeArguments="viewModels:NewsfeedItemViewModel"
                      xmlns="http://xamarin.com/schemas/2014/forms" 
                      xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
                      xmlns:views="clr-namespace:MvvmCross.Forms.Views;assembly=MvvmCross.Forms"
                      xmlns:mvx="clr-namespace:MvvmCross.Forms.Bindings;assembly=MvvmCross.Forms"
                      xmlns:viewModels="clr-namespace:Surrender_20.Forms.ViewModels;assembly=Surrender_20.Forms"
                      xmlns:conv="clr-namespace:Surrender_20.Forms.Converters"
                      x:Class="Surrender_20.Forms.Views.NewsfeedItemView"
                      Title="{Binding Path=Title}">

    <views:MvxContentPage.Resources>
        <ResourceDictionary>
            <conv:NegateBoolConverter x:Key="NegateBoolConverter"/>
        </ResourceDictionary>
    </views:MvxContentPage.Resources>

    <views:MvxContentPage.Content>
        <Grid CompressedLayout.IsHeadless="true">
            <ActivityIndicator Style="{StaticResource ActivityIndicator}"/>

            <ScrollView IsVisible="{Binding IsLoading, Converter={StaticResource NegateBoolConverter}}">
                <StackLayout>
                    <Image Source="{Binding Path=ThumbnailSource}"
                           Aspect="AspectFill"/>

                    <ContentView Content="{Binding Content}"
                                 Margin="15,10,15,10"/>
                </StackLayout>

            </ScrollView>
        </Grid>
    </views:MvxContentPage.Content>
</views:MvxContentPage>