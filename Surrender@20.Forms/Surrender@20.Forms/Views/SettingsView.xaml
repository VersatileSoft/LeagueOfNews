<views:MvxContentPage x:TypeArguments="viewModels:SettingsViewModel"
                      xmlns="http://xamarin.com/schemas/2014/forms" 
                      xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
                      xmlns:views="clr-namespace:MvvmCross.Forms.Views;assembly=MvvmCross.Forms"
                      xmlns:mvx="clr-namespace:MvvmCross.Forms.Bindings;assembly=MvvmCross.Forms"
                      xmlns:viewModels="clr-namespace:Surrender_20.Forms.ViewModels;assembly=Surrender_20.Forms"
                      x:Class="Surrender_20.Forms.Views.SettingsView"
                      BackgroundColor="#001119"
                      Title="Settings">

    <views:MvxContentPage.Content>
        <StackLayout Spacing="15" Padding="15">
            <Frame Style="{StaticResource Frame}" Padding="10" HorizontalOptions="FillAndExpand">
                <StackLayout>
                    <Label Text="Notifications" TextColor="White" FontSize="Large"/>
                    <Switch IsToggled="{Binding IsNotificationsEnabled, Mode=TwoWay}" IsEnabled="True"/>
                    <Label Text="Notifications delay" TextColor="White" FontSize="Large"/>
                    <Picker Title="Select delay" ItemsSource="{Binding DelayList}" SelectedItem="{Binding Delay, Mode=TwoWay}" TextColor="White" FontSize="Large"/>
                </StackLayout>
            </Frame>

            <Frame Style="{StaticResource Frame}" Padding="10" HorizontalOptions="FillAndExpand">
                <StackLayout>
                    <Label Text="Dark theme" TextColor="White" FontSize="Large"/>
                    <Switch IsToggled="{Binding DarkTheme, Mode=TwoWay}" IsEnabled="True"/>
                </StackLayout>
            </Frame>

            <Frame Style="{StaticResource Frame}" Padding="10" HorizontalOptions="FillAndExpand">
                <StackLayout>
                    <Label Text="League of News v1.0" TextColor="White" FontSize="Large"/>
                    <Label Text="Versatile Software 2019" TextColor="Gray" FontSize="Medium"/>
                    <!--<Label Text="Marek Kida, Michał Woźniak and Kacper Tryniecki" TextColor="White" FontSize="Large"/>-->
                </StackLayout>
            </Frame>
        </StackLayout>
    </views:MvxContentPage.Content>
</views:MvxContentPage>