<Frame x:Class="LeagueOfNews.Forms.Controls.NewsfeedItemCell" Style="{DynamicResource Frame}"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:LeagueOfNews.Model="clr-namespace:LeagueOfNews.Model;assembly=LeagueOfNews.Core"
    xmlns:d="http://xamarin.com/schemas/2014/forms/design"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:util="clr-namespace:LeagueOfNews.Forms.Views.Utils"
    Margin="10,5,10,5"
    Padding="10"
    x:DataType="LeagueOfNews.Model:Newsfeed"
    Visual="Material"
    mc:Ignorable="d">

    <Grid>
        <StackLayout>
            <util:ExtendedLabel Style="{StaticResource TitleExtendedLabel}" Text="{x:Binding Title}"/>

            <Label Style="{StaticResource DateLabel}"
                Text="{x:Binding Date}"
                TextColor="{DynamicResource Label}"/>

            <Label Style="{DynamicResource DescriptionLabel}"
                MaxLines="4"
                Text="{x:Binding ShortDescription}"/>
        </StackLayout>
    </Grid>
</Frame>